// module 16
export function m16_f0(x: number): number {
  let acc = x + 16 * 0;
  acc = (acc * 2 + 16) % 1000003;
  acc = (acc * 3 + 17) % 1000003;
  acc = (acc * 4 + 18) % 1000003;
  acc = (acc * 5 + 19) % 1000003;
  acc = (acc * 6 + 20) % 1000003;
  acc = (acc * 7 + 21) % 1000003;
  return acc;
}
export function m16_f1(x: number): number {
  let acc = x + 16 * 1;
  acc = (acc * 2 + 17) % 1000003;
  acc = (acc * 3 + 18) % 1000003;
  acc = (acc * 4 + 19) % 1000003;
  acc = (acc * 5 + 20) % 1000003;
  acc = (acc * 6 + 21) % 1000003;
  acc = (acc * 7 + 22) % 1000003;
  return acc;
}
export function m16_f2(x: number): number {
  let acc = x + 16 * 2;
  acc = (acc * 2 + 18) % 1000003;
  acc = (acc * 3 + 19) % 1000003;
  acc = (acc * 4 + 20) % 1000003;
  acc = (acc * 5 + 21) % 1000003;
  acc = (acc * 6 + 22) % 1000003;
  acc = (acc * 7 + 23) % 1000003;
  return acc;
}
export function m16_f3(x: number): number {
  let acc = x + 16 * 3;
  acc = (acc * 2 + 19) % 1000003;
  acc = (acc * 3 + 20) % 1000003;
  acc = (acc * 4 + 21) % 1000003;
  acc = (acc * 5 + 22) % 1000003;
  acc = (acc * 6 + 23) % 1000003;
  acc = (acc * 7 + 24) % 1000003;
  return acc;
}
export function m16_f4(x: number): number {
  let acc = x + 16 * 4;
  acc = (acc * 2 + 20) % 1000003;
  acc = (acc * 3 + 21) % 1000003;
  acc = (acc * 4 + 22) % 1000003;
  acc = (acc * 5 + 23) % 1000003;
  acc = (acc * 6 + 24) % 1000003;
  acc = (acc * 7 + 25) % 1000003;
  return acc;
}
export function m16_f5(x: number): number {
  let acc = x + 16 * 5;
  acc = (acc * 2 + 21) % 1000003;
  acc = (acc * 3 + 22) % 1000003;
  acc = (acc * 4 + 23) % 1000003;
  acc = (acc * 5 + 24) % 1000003;
  acc = (acc * 6 + 25) % 1000003;
  acc = (acc * 7 + 26) % 1000003;
  return acc;
}
export function m16_f6(x: number): number {
  let acc = x + 16 * 6;
  acc = (acc * 2 + 22) % 1000003;
  acc = (acc * 3 + 23) % 1000003;
  acc = (acc * 4 + 24) % 1000003;
  acc = (acc * 5 + 25) % 1000003;
  acc = (acc * 6 + 26) % 1000003;
  acc = (acc * 7 + 27) % 1000003;
  return acc;
}
export function m16_f7(x: number): number {
  let acc = x + 16 * 7;
  acc = (acc * 2 + 23) % 1000003;
  acc = (acc * 3 + 24) % 1000003;
  acc = (acc * 4 + 25) % 1000003;
  acc = (acc * 5 + 26) % 1000003;
  acc = (acc * 6 + 27) % 1000003;
  acc = (acc * 7 + 28) % 1000003;
  return acc;
}
