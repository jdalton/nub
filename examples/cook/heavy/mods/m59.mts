// module 59
export function m59_f0(x: number): number {
  let acc = x + 59 * 0;
  acc = (acc * 2 + 59) % 1000003;
  acc = (acc * 3 + 60) % 1000003;
  acc = (acc * 4 + 61) % 1000003;
  acc = (acc * 5 + 62) % 1000003;
  acc = (acc * 6 + 63) % 1000003;
  acc = (acc * 7 + 64) % 1000003;
  return acc;
}
export function m59_f1(x: number): number {
  let acc = x + 59 * 1;
  acc = (acc * 2 + 60) % 1000003;
  acc = (acc * 3 + 61) % 1000003;
  acc = (acc * 4 + 62) % 1000003;
  acc = (acc * 5 + 63) % 1000003;
  acc = (acc * 6 + 64) % 1000003;
  acc = (acc * 7 + 65) % 1000003;
  return acc;
}
export function m59_f2(x: number): number {
  let acc = x + 59 * 2;
  acc = (acc * 2 + 61) % 1000003;
  acc = (acc * 3 + 62) % 1000003;
  acc = (acc * 4 + 63) % 1000003;
  acc = (acc * 5 + 64) % 1000003;
  acc = (acc * 6 + 65) % 1000003;
  acc = (acc * 7 + 66) % 1000003;
  return acc;
}
export function m59_f3(x: number): number {
  let acc = x + 59 * 3;
  acc = (acc * 2 + 62) % 1000003;
  acc = (acc * 3 + 63) % 1000003;
  acc = (acc * 4 + 64) % 1000003;
  acc = (acc * 5 + 65) % 1000003;
  acc = (acc * 6 + 66) % 1000003;
  acc = (acc * 7 + 67) % 1000003;
  return acc;
}
export function m59_f4(x: number): number {
  let acc = x + 59 * 4;
  acc = (acc * 2 + 63) % 1000003;
  acc = (acc * 3 + 64) % 1000003;
  acc = (acc * 4 + 65) % 1000003;
  acc = (acc * 5 + 66) % 1000003;
  acc = (acc * 6 + 67) % 1000003;
  acc = (acc * 7 + 68) % 1000003;
  return acc;
}
export function m59_f5(x: number): number {
  let acc = x + 59 * 5;
  acc = (acc * 2 + 64) % 1000003;
  acc = (acc * 3 + 65) % 1000003;
  acc = (acc * 4 + 66) % 1000003;
  acc = (acc * 5 + 67) % 1000003;
  acc = (acc * 6 + 68) % 1000003;
  acc = (acc * 7 + 69) % 1000003;
  return acc;
}
export function m59_f6(x: number): number {
  let acc = x + 59 * 6;
  acc = (acc * 2 + 65) % 1000003;
  acc = (acc * 3 + 66) % 1000003;
  acc = (acc * 4 + 67) % 1000003;
  acc = (acc * 5 + 68) % 1000003;
  acc = (acc * 6 + 69) % 1000003;
  acc = (acc * 7 + 70) % 1000003;
  return acc;
}
export function m59_f7(x: number): number {
  let acc = x + 59 * 7;
  acc = (acc * 2 + 66) % 1000003;
  acc = (acc * 3 + 67) % 1000003;
  acc = (acc * 4 + 68) % 1000003;
  acc = (acc * 5 + 69) % 1000003;
  acc = (acc * 6 + 70) % 1000003;
  acc = (acc * 7 + 71) % 1000003;
  return acc;
}
