// module 2
export function m2_f0(x: number): number {
  let acc = x + 2 * 0;
  acc = (acc * 2 + 2) % 1000003;
  acc = (acc * 3 + 3) % 1000003;
  acc = (acc * 4 + 4) % 1000003;
  acc = (acc * 5 + 5) % 1000003;
  acc = (acc * 6 + 6) % 1000003;
  acc = (acc * 7 + 7) % 1000003;
  return acc;
}
export function m2_f1(x: number): number {
  let acc = x + 2 * 1;
  acc = (acc * 2 + 3) % 1000003;
  acc = (acc * 3 + 4) % 1000003;
  acc = (acc * 4 + 5) % 1000003;
  acc = (acc * 5 + 6) % 1000003;
  acc = (acc * 6 + 7) % 1000003;
  acc = (acc * 7 + 8) % 1000003;
  return acc;
}
export function m2_f2(x: number): number {
  let acc = x + 2 * 2;
  acc = (acc * 2 + 4) % 1000003;
  acc = (acc * 3 + 5) % 1000003;
  acc = (acc * 4 + 6) % 1000003;
  acc = (acc * 5 + 7) % 1000003;
  acc = (acc * 6 + 8) % 1000003;
  acc = (acc * 7 + 9) % 1000003;
  return acc;
}
export function m2_f3(x: number): number {
  let acc = x + 2 * 3;
  acc = (acc * 2 + 5) % 1000003;
  acc = (acc * 3 + 6) % 1000003;
  acc = (acc * 4 + 7) % 1000003;
  acc = (acc * 5 + 8) % 1000003;
  acc = (acc * 6 + 9) % 1000003;
  acc = (acc * 7 + 10) % 1000003;
  return acc;
}
export function m2_f4(x: number): number {
  let acc = x + 2 * 4;
  acc = (acc * 2 + 6) % 1000003;
  acc = (acc * 3 + 7) % 1000003;
  acc = (acc * 4 + 8) % 1000003;
  acc = (acc * 5 + 9) % 1000003;
  acc = (acc * 6 + 10) % 1000003;
  acc = (acc * 7 + 11) % 1000003;
  return acc;
}
export function m2_f5(x: number): number {
  let acc = x + 2 * 5;
  acc = (acc * 2 + 7) % 1000003;
  acc = (acc * 3 + 8) % 1000003;
  acc = (acc * 4 + 9) % 1000003;
  acc = (acc * 5 + 10) % 1000003;
  acc = (acc * 6 + 11) % 1000003;
  acc = (acc * 7 + 12) % 1000003;
  return acc;
}
export function m2_f6(x: number): number {
  let acc = x + 2 * 6;
  acc = (acc * 2 + 8) % 1000003;
  acc = (acc * 3 + 9) % 1000003;
  acc = (acc * 4 + 10) % 1000003;
  acc = (acc * 5 + 11) % 1000003;
  acc = (acc * 6 + 12) % 1000003;
  acc = (acc * 7 + 13) % 1000003;
  return acc;
}
export function m2_f7(x: number): number {
  let acc = x + 2 * 7;
  acc = (acc * 2 + 9) % 1000003;
  acc = (acc * 3 + 10) % 1000003;
  acc = (acc * 4 + 11) % 1000003;
  acc = (acc * 5 + 12) % 1000003;
  acc = (acc * 6 + 13) % 1000003;
  acc = (acc * 7 + 14) % 1000003;
  return acc;
}
