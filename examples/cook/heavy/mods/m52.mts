// module 52
export function m52_f0(x: number): number {
  let acc = x + 52 * 0;
  acc = (acc * 2 + 52) % 1000003;
  acc = (acc * 3 + 53) % 1000003;
  acc = (acc * 4 + 54) % 1000003;
  acc = (acc * 5 + 55) % 1000003;
  acc = (acc * 6 + 56) % 1000003;
  acc = (acc * 7 + 57) % 1000003;
  return acc;
}
export function m52_f1(x: number): number {
  let acc = x + 52 * 1;
  acc = (acc * 2 + 53) % 1000003;
  acc = (acc * 3 + 54) % 1000003;
  acc = (acc * 4 + 55) % 1000003;
  acc = (acc * 5 + 56) % 1000003;
  acc = (acc * 6 + 57) % 1000003;
  acc = (acc * 7 + 58) % 1000003;
  return acc;
}
export function m52_f2(x: number): number {
  let acc = x + 52 * 2;
  acc = (acc * 2 + 54) % 1000003;
  acc = (acc * 3 + 55) % 1000003;
  acc = (acc * 4 + 56) % 1000003;
  acc = (acc * 5 + 57) % 1000003;
  acc = (acc * 6 + 58) % 1000003;
  acc = (acc * 7 + 59) % 1000003;
  return acc;
}
export function m52_f3(x: number): number {
  let acc = x + 52 * 3;
  acc = (acc * 2 + 55) % 1000003;
  acc = (acc * 3 + 56) % 1000003;
  acc = (acc * 4 + 57) % 1000003;
  acc = (acc * 5 + 58) % 1000003;
  acc = (acc * 6 + 59) % 1000003;
  acc = (acc * 7 + 60) % 1000003;
  return acc;
}
export function m52_f4(x: number): number {
  let acc = x + 52 * 4;
  acc = (acc * 2 + 56) % 1000003;
  acc = (acc * 3 + 57) % 1000003;
  acc = (acc * 4 + 58) % 1000003;
  acc = (acc * 5 + 59) % 1000003;
  acc = (acc * 6 + 60) % 1000003;
  acc = (acc * 7 + 61) % 1000003;
  return acc;
}
export function m52_f5(x: number): number {
  let acc = x + 52 * 5;
  acc = (acc * 2 + 57) % 1000003;
  acc = (acc * 3 + 58) % 1000003;
  acc = (acc * 4 + 59) % 1000003;
  acc = (acc * 5 + 60) % 1000003;
  acc = (acc * 6 + 61) % 1000003;
  acc = (acc * 7 + 62) % 1000003;
  return acc;
}
export function m52_f6(x: number): number {
  let acc = x + 52 * 6;
  acc = (acc * 2 + 58) % 1000003;
  acc = (acc * 3 + 59) % 1000003;
  acc = (acc * 4 + 60) % 1000003;
  acc = (acc * 5 + 61) % 1000003;
  acc = (acc * 6 + 62) % 1000003;
  acc = (acc * 7 + 63) % 1000003;
  return acc;
}
export function m52_f7(x: number): number {
  let acc = x + 52 * 7;
  acc = (acc * 2 + 59) % 1000003;
  acc = (acc * 3 + 60) % 1000003;
  acc = (acc * 4 + 61) % 1000003;
  acc = (acc * 5 + 62) % 1000003;
  acc = (acc * 6 + 63) % 1000003;
  acc = (acc * 7 + 64) % 1000003;
  return acc;
}
