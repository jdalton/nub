// module 4
export function m4_f0(x: number): number {
  let acc = x + 4 * 0;
  acc = (acc * 2 + 4) % 1000003;
  acc = (acc * 3 + 5) % 1000003;
  acc = (acc * 4 + 6) % 1000003;
  acc = (acc * 5 + 7) % 1000003;
  acc = (acc * 6 + 8) % 1000003;
  acc = (acc * 7 + 9) % 1000003;
  return acc;
}
export function m4_f1(x: number): number {
  let acc = x + 4 * 1;
  acc = (acc * 2 + 5) % 1000003;
  acc = (acc * 3 + 6) % 1000003;
  acc = (acc * 4 + 7) % 1000003;
  acc = (acc * 5 + 8) % 1000003;
  acc = (acc * 6 + 9) % 1000003;
  acc = (acc * 7 + 10) % 1000003;
  return acc;
}
export function m4_f2(x: number): number {
  let acc = x + 4 * 2;
  acc = (acc * 2 + 6) % 1000003;
  acc = (acc * 3 + 7) % 1000003;
  acc = (acc * 4 + 8) % 1000003;
  acc = (acc * 5 + 9) % 1000003;
  acc = (acc * 6 + 10) % 1000003;
  acc = (acc * 7 + 11) % 1000003;
  return acc;
}
export function m4_f3(x: number): number {
  let acc = x + 4 * 3;
  acc = (acc * 2 + 7) % 1000003;
  acc = (acc * 3 + 8) % 1000003;
  acc = (acc * 4 + 9) % 1000003;
  acc = (acc * 5 + 10) % 1000003;
  acc = (acc * 6 + 11) % 1000003;
  acc = (acc * 7 + 12) % 1000003;
  return acc;
}
export function m4_f4(x: number): number {
  let acc = x + 4 * 4;
  acc = (acc * 2 + 8) % 1000003;
  acc = (acc * 3 + 9) % 1000003;
  acc = (acc * 4 + 10) % 1000003;
  acc = (acc * 5 + 11) % 1000003;
  acc = (acc * 6 + 12) % 1000003;
  acc = (acc * 7 + 13) % 1000003;
  return acc;
}
export function m4_f5(x: number): number {
  let acc = x + 4 * 5;
  acc = (acc * 2 + 9) % 1000003;
  acc = (acc * 3 + 10) % 1000003;
  acc = (acc * 4 + 11) % 1000003;
  acc = (acc * 5 + 12) % 1000003;
  acc = (acc * 6 + 13) % 1000003;
  acc = (acc * 7 + 14) % 1000003;
  return acc;
}
export function m4_f6(x: number): number {
  let acc = x + 4 * 6;
  acc = (acc * 2 + 10) % 1000003;
  acc = (acc * 3 + 11) % 1000003;
  acc = (acc * 4 + 12) % 1000003;
  acc = (acc * 5 + 13) % 1000003;
  acc = (acc * 6 + 14) % 1000003;
  acc = (acc * 7 + 15) % 1000003;
  return acc;
}
export function m4_f7(x: number): number {
  let acc = x + 4 * 7;
  acc = (acc * 2 + 11) % 1000003;
  acc = (acc * 3 + 12) % 1000003;
  acc = (acc * 4 + 13) % 1000003;
  acc = (acc * 5 + 14) % 1000003;
  acc = (acc * 6 + 15) % 1000003;
  acc = (acc * 7 + 16) % 1000003;
  return acc;
}
